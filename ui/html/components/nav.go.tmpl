{{define "nav"}}
<nav>
    <div>
        <a href='/'>Home</a>
         {{if .IsAuthenticated}}
            <a href='/'>Add Feed</a>
        {{end}}
    </div>
    <div>
        {{if .IsAuthenticated}}
            <form action='/logout' method='POST'>
                <button>Logout</button>
            </form>
        {{else}}
            <a href='/signup'>Signup</a>
            <a href='/login'>Login</a>
        {{end}}
    </div>
</nav>
{{end}}