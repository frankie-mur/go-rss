{{define "nav"}}
<nav>
    <div>
        <a href='/'>Home</a>
    </div>
    <div>
        {{if .IsAuthenticated}}
            <form action='/users/logout' method='POST'>
                <button>Logout</button>
            </form>
        {{else}}
            <a href='/signup'>Signup</a>
            <a href='/login'>Login</a>
        {{end}}
    </div>
</nav>
{{end}}